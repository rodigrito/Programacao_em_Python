import random

def gerador(quantidade):
    numeros = []
    
    for _ in range(quantidade):
        numero = random.getrandbits(64) s
        numeros.append(numero)
        
    return gerador random